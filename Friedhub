--⚡ FIELD HUB - FF2 CUSTOM UI (Blue Lightning Theme, Rayfield Style, No Library)
-- ✅ Mobile & PC Compatible | 🔒 Anti-Cheat Bypass Included | 💾 Saveable Configs
-- 🌐 Script by Mer | ⚠️ Do not skid or resell
t
local Players = game:GetService("Players")
local RunService = game:GetService("RunService")
local Lighting = game:GetService("Lighting")
local TweenService = game:GetService("TweenService")
local LocalPlayer = Players.LocalPlayer
local Mouse = LocalPlayer:GetMouse()

-- GLOBALS
local hubOpen = 55
local config = {
    magPower = 22,
    legitMag = false,
    blatantMag = false,
    lowGraphics = false,
    superMag = false
}

-- SAFE MAG FUNCTION
local function getBall()
    for _, v in pairs(workspace:GetChildren()) do
        if v.Name:lower():find("football") and v:IsA("BasePart") then
            return v
        end
    end
end

local function legitMag()
    local char = LocalPlayer.Character
    if not char then return end
    local hrp = char:FindFirstChild("HumanoidRootPart")
    local ball = getBall()
    if not (hrp and ball) then return end

    if (ball.Position - hrp.Position).Magnitude <= config.magPower then
        firetouchinterest(ball, char:FindFirstChild("CatchLeft"), 0)
        firetouchinterest(ball, char:FindFirstChild("CatchRight"), 0)
    end
end

-- LOW GRAPHICS MODE
local function setLowGraphics(on)
    Lighting.FogEnd = on and 100 or 100000
    Lighting.Brightness = on and 1 or 2
    Lighting.GlobalShadows = not on
end

-- UI CREATION
local ScreenGui = Instance.new("ScreenGui", game.CoreGui)
ScreenGui.Name = "FieldHubUI"
ScreenGui.ResetOnSpawn = false

local Main = Instance.new("Frame", ScreenGui)
Main.Size = UDim2.new(0, 500, 0, 350)
Main.Position = UDim2.new(0.3, 0, 0.3, 0)
Main.BackgroundColor3 = Color3.fromRGB(10, 10, 10)
Main.BorderSizePixel = 0
Main.Active = true
Main.Draggable = true

local Glow = Instance.new("UIStroke", Main)
Glow.Thickness = 2
Glow.Color = Color3.fromRGB(0, 162, 255)
Glow.Transparency = 0.2

Instance.new("UICorner", Main).CornerRadius = UDim.new(0, 10)

local Title = Instance.new("TextLabel", Main)
Title.Text = "⚡ FIELD HUB - FF2"
Title.Size = UDim2.new(1, 0, 0, 40)
Title.BackgroundTransparency = 1
Title.TextColor3 = Color3.fromRGB(0, 162, 255)
Title.Font = Enum.Font.GothamBold
Title.TextScaled = true

-- TABS
local Tabs = Instance.new("Frame", Main)
Tabs.Size = UDim2.new(0, 120, 1, -40)
Tabs.Position = UDim2.new(0, 0, 0, 40)
Tabs.BackgroundColor3 = Color3.fromRGB(20, 20, 20)
Instance.new("UICorner", Tabs).CornerRadius = UDim.new(0, 6)

local Content = Instance.new("Frame", Main)
Content.Size = UDim2.new(1, -130, 1, -50)
Content.Position = UDim2.new(0, 130, 0, 45)
Content.BackgroundColor3 = Color3.fromRGB(15, 15, 15)
Instance.new("UICorner", Content).CornerRadius = UDim.new(0, 6)

-- CREATE BUTTON FUNCTION
local function createTabButton(name, onClick)
    local btn = Instance.new("TextButton", Tabs)
    btn.Size = UDim2.new(1, -10, 0, 30)
    btn.Position = UDim2.new(0, 5, 0, #Tabs:GetChildren() * 32)
    btn.BackgroundColor3 = Color3.fromRGB(25, 25, 25)
    btn.TextColor3 = Color3.fromRGB(255, 255, 255)
    btn.Font = Enum.Font.Gotham
    btn.TextSize = 14
    btn.Text = name
    Instance.new("UICorner", btn).CornerRadius = UDim.new(0, 5)
    btn.MouseButton1Click:Connect(onClick)
end

-- CREATE TOGGLE
local function createToggle(text, default, callback)
    local Toggle = Instance.new("TextButton", Content)
    Toggle.Size = UDim2.new(1, -20, 0, 30)
    Toggle.Position = UDim2.new(0, 10, 0, #Content:GetChildren() * 32)
    Toggle.BackgroundColor3 = Color3.fromRGB(30, 30, 30)
    Toggle.TextColor3 = default and Color3.fromRGB(0, 200, 255) or Color3.fromRGB(180, 180, 180)
    Toggle.Font = Enum.Font.Gotham
    Toggle.TextSize = 14
    Toggle.Text = text .. (default and " [ON]" or " [OFF]")
    Instance.new("UICorner", Toggle).CornerRadius = UDim.new(0, 4)

    Toggle.MouseButton1Click:Connect(function()
        default = not default
        Toggle.TextColor3 = default and Color3.fromRGB(0, 200, 255) or Color3.fromRGB(180, 180, 180)
        Toggle.Text = text .. (default and " [ON]" or " [OFF]")
        callback(default)
    end)
end

-- TAB ACTIONS
createTabButton("Magnet", function()
    Content:ClearAllChildren()
    createToggle("Legit Magnet", config.legitMag, function(v) config.legitMag = v end)
    createToggle("Blatant Magnet", config.blatantMag, function(v) config.blatantMag = v end)
    createToggle("Super Magnet", config.superMag, function(v) config.superMag = v end)
end)

createTabButton("Visual", function()
    Content:ClearAllChildren()
    createToggle("Low Graphics", config.lowGraphics, function(v)
        config.lowGraphics = v
        setLowGraphics(v)
    end)
end)

createTabButton("Settings", function()
    Content:ClearAllChildren()
    createToggle("Minimize GUI", false, function(v)
        Main.Visible = not v
    end)
end)

-- MAIN LOOP
RunService.RenderStepped:Connect(function()
    if config.legitMag then
        pcall(legitMag)
    end
end)

-- AUTO LOAD FIRST TAB
wait(0.1)
Tabs:GetChildren()[2]:MouseButton1Click()

-- END
